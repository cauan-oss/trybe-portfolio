const express = require('express');

const app = express();

// criando uma funcao de leitura para o movies.json

// 1-passo importar os modulos fs e path 
const fs = require('fs').promises;
const path = require('path');

module.exports = app;